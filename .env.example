PORT=3000
HOST=0.0.0.0
API_KEY=replace-with-a-long-random-secret
DATABASE_URL=postgresql://waconnect:password@localhost:5432/waconnect
DATABASE_POOL_MAX=10
# Enable only when your managed PostgreSQL provider requires TLS.
DATABASE_SSL=false
LOG_LEVEL=info
# Set true only for local development. Never use on a public server.
ALLOW_INSECURE=false
